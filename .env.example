ANNAS_SECRET_KEY="your-api-key"

# Optional. Overrides auto-discovered mirror (e.g. annas-archive.li).
# ANNAS_BASE_URL="annas-archive.li"

# Optional. Absolute path for book file downloads (book_download with download:true).
# ANNAS_DOWNLOAD_PATH="/path/to/downloads"
