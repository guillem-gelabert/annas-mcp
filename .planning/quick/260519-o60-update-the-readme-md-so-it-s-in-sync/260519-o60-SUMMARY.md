---
phase: quick
plan: 260519-o60
subsystem: docs
tags: [readme, docs, v1.2, book-tools]
dependency_graph:
  requires: []
  provides: [up-to-date README for v1.2]
  affects: [README.md]
tech_stack:
  added: []
  patterns: []
key_files:
  created: []
  modified:
    - README.md
decisions:
  - README reflects v1.2 scope; "v1 Scope" renamed to "Features" with book tools listed
  - ANNAS_DOWNLOAD_PATH description updated to cover both article_download and book_download
  - Porting Matrix book rows updated from "Do not port in v1" to "Port / Shipped in v1.2"
metrics:
  duration: "~3 minutes"
  completed: "2026-05-19"
  tasks_completed: 1
  files_changed: 1
---

# Phase quick Plan 260519-o60: Update README.md to Reflect v1.2 State Summary

**One-liner:** README now documents all five v1.2 tools (article_search, article_download, book_search, book_download, update_base_url) with full parameter and output schemas.

## What Was Done

Updated `README.md` in place to match the shipped v1.2 codebase:

1. **Intro / title**: Replaced "article lookup" with "article and book lookup"; removed the "v1 is intentionally narrow" sentence.

2. **"v1 Scope" → "Features"**: Added `book_search` and `book_download` to the Included list. Removed the "Not included in v1: Book tools" line. Kept "CLI commands" and "Standalone binary packaging" in the Not included block.

3. **ANNAS_DOWNLOAD_PATH**: Updated description to state it applies to both `article_download` and `book_download`.

4. **book_search tool section**: Added after `article_download` — query input, structured output example with all optional fields (`language`, `format`, `size`, `title`, `publisher`, `authors`, `pageUrl`, `hash`).

5. **book_download tool section**: Added after `book_search` — all five parameters documented (`hash`, `title`, `format`, `download`, `downloadPath`), fast_download-only note, output fields (`book`, `fastDownloadUrl`, `filePath`), representative JSON examples.

6. **Upstream Porting Matrix**: Changed `book_search` and `book_download` rows from "Do not port in v1 / Article-only v1 scope." to "Port / Shipped in v1.2." Updated `ANNAS_DOWNLOAD_PATH` row reason to mention both download tools.

## Commits

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Rewrite README.md to reflect v1.2 state | 9f71c86 | README.md |

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Threat Flags

None — documentation-only change, no new code surface.

## Self-Check: PASSED

- README.md exists and contains 6 occurrences of "book_search" and 6 of "book_download"
- No "Do not port in v1" rows remain for book tools
- ANNAS_DOWNLOAD_PATH description references both article_download and book_download
- Commit 9f71c86 verified in git log
