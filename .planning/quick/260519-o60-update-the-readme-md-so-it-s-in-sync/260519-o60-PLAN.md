---
phase: quick
plan: 260519-o60
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: []

must_haves:
  truths:
    - "README title and intro describe v1.2 (book tools shipped, not article-only)"
    - "v1 Scope section lists all 5 tools and removes 'Not included in v1' section for book tools"
    - "book_search tool is documented with input/output schema and parameters"
    - "book_download tool is documented with all parameters (hash, title, format, download, downloadPath) and output"
    - "ANNAS_DOWNLOAD_PATH env var section notes it applies to both article and book downloads"
    - "Upstream Porting Matrix rows for book_search and book_download are updated from 'Do not port in v1' to 'Port'"
  artifacts:
    - path: README.md
      provides: "Up-to-date v1.2 documentation"
  key_links:
    - from: README.md
      to: src/tools/book-tools.ts
      via: "book_search and book_download tool docs match actual schemas"
---

<objective>
Update README.md to reflect the current v1.2 state of the project.

Purpose: The README still describes v1 as article-only. v1.2 shipped book_search and book_download. The README needs to match what is actually in the codebase.
Output: A single updated README.md file — no other files touched.
</objective>

<execution_context>
@/Users/guillem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guillem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/guillem/vault/projects/_tools/annas-mcp/.planning/PROJECT.md
@/Users/guillem/vault/projects/_tools/annas-mcp/README.md
@/Users/guillem/vault/projects/_tools/annas-mcp/src/tools/book-tools.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md to reflect v1.2 state</name>
  <files>README.md</files>
  <action>
Rewrite README.md in place. Keep all existing sections that are still accurate; update only what has changed. Specific changes required:

1. **Title / intro paragraph**: Change "Bun TypeScript MCP server for Anna's Archive article lookup." to reflect that the server handles both articles and books. Remove "v1 is intentionally narrow: it exposes only article tools". Replace with a neutral statement that it exposes MCP tools for searching and downloading articles and books.

2. **"v1 Scope" section**: Rename to "Features" or "What's included". Remove the "Not included in v1" block that listed book tools and CLI. Add `book_search` and `book_download` to the Included list. Keep "CLI commands" and "Standalone binary packaging" in a "Not included" block (they are still out of scope).

3. **"ANNAS_DOWNLOAD_PATH" env var description** (in Configure section): The current text says "when `article_download` is called with `download: true`". Update to say it applies to both `article_download` and `book_download`.

4. **Tools section — add `book_search`** after `article_download`. Document:
   - Input: `{ "query": "string" }` — title, author, ISBN, or keywords
   - Output: structured results array where each item has: `language`, `format`, `size`, `title`, `publisher`, `authors`, `pageUrl`, `hash`. All fields are optional strings.
   - Show a representative JSON output example.

5. **Tools section — add `book_download`** after `book_search`. Document:
   - Parameters:
     - `hash` (required): MD5 hash from book_search results
     - `title` (optional): used for filename when downloading
     - `format` (optional): file format (e.g. EPUB, PDF), used as file extension
     - `download` (optional, default false): if true, write file to disk
     - `downloadPath` (optional, absolute path): per-call override of ANNAS_DOWNLOAD_PATH
   - Note: book_download uses fast_download only (no SciDB fallback — books do not have one).
   - Note: when download is not set, only fastDownloadUrl is returned — no file is written.
   - Output fields: `book` (book metadata object), `fastDownloadUrl` (string), `filePath` (string, only present if download: true succeeded)
   - Show representative JSON input and output examples.

6. **Upstream Porting Matrix**: Update the two rows for `book_search` and `book_download` from "Do not port in v1 / Article-only v1 scope." to "Port / Shipped in v1.2." Keep all other rows unchanged.

Do NOT add any new sections, change the document structure beyond the above, or alter the Responsible Use section.
  </action>
  <verify>
    <automated>grep -c "book_search" /Users/guillem/vault/projects/_tools/annas-mcp/README.md</automated>
  </verify>
  <done>
README.md contains documentation for both book_search and book_download tools, the scope section no longer says book tools are excluded, ANNAS_DOWNLOAD_PATH docs cover both download tools, and the Porting Matrix rows for book tools say "Port" not "Do not port".
  </done>
</task>

</tasks>

<verification>
After writing the file:
- grep "book_search" README.md — must appear in Tools section, not just Porting Matrix
- grep "book_download" README.md — same
- grep "Do not port in v1" README.md — must return 0 matches for the book rows
- grep "ANNAS_DOWNLOAD_PATH" README.md — description must reference both article and book downloads
</verification>

<success_criteria>
README.md accurately describes the v1.2 state: 5 tools documented (article_search, article_download, book_search, book_download, update_base_url), scope section updated, env var docs updated, Porting Matrix updated.
</success_criteria>

<output>
Create `.planning/quick/260519-o60-update-the-readme-md-so-it-s-in-sync/260519-o60-SUMMARY.md` when done.
</output>
