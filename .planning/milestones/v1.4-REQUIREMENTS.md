# Requirements Archive: annas-mcp-ts v1.4 DOI Verification

**Milestone:** v1.4 DOI Verification
**Archived:** 2026-05-28
**Status:** ✅ COMPLETE — All 11 requirements satisfied

---

## v1.4 Requirements

### CrossRef Integration

- [x] **VER-01**: After Anna's Archive resolves a DOI to metadata, fetch the canonical title from CrossRef (`https://api.crossref.org/works/{doi}`) using the `title` field in the response
- [x] **VER-02**: CrossRef fetch uses a descriptive `User-Agent` header per CrossRef etiquette (e.g. `annas-mcp-ts/1.4 (mailto:noreply@example.com)`)
- [x] **VER-03**: CrossRef fetch has a timeout (≤ 5 seconds) and does not block or delay the Anna's Archive sources being returned

### Title Comparison

- [x] **VER-04**: Title comparison is case-insensitive and strips punctuation before comparing
- [x] **VER-05**: Confidence is `"high"` when normalized word-overlap (Jaccard similarity on word sets) is ≥ 0.5, `"low"` when < 0.5
- [x] **VER-06**: Confidence is `"unverified"` when CrossRef is unreachable, times out, returns a non-200 status, or returns no title for the DOI

### Response Shape (as delivered — updated 2026-05-28)

- [x] **VER-07**: Single-DOI `article_download` succeeds (returns `article` + `sources`) only when confidence is `"high"`; `"low"` confidence returns `isError: true` with a title-mismatch message; `"unverified"` returns `isError: true` with a cannot-verify message — no `verification` field in the response
- [x] **VER-08**: Batch `article_download` applies the same gating per article: high-confidence items return `{ doi, article, sources }`; low/unverified items return `{ doi, error }` — batch continues processing remaining DOIs regardless
- [x] **VER-09**: CrossRef network failures are absorbed (`fetchCrossRefTitle` catches `TimeoutError` / `TypeError`; handler wraps call in `.catch(() => null)`) — `null` crossrefTitle routes to `computeConfidence(title, null)` → `"unverified"` → `isError: true` error response; the server never throws or crashes on CrossRef unavailability

### Backwards Compatibility

- [x] **VER-10**: Single-DOI response shape is additive only — no existing fields removed or renamed (net effect zero: `verificationSchema` was added in Phase 17 GREEN then removed by post-phase upgrade)
- [x] **VER-11**: All existing tests continue to pass; the verification behavior is testable via a mock CrossRef fetch implementation

---

## Requirement Outcomes

| REQ-ID | Phase | Outcome | Notes |
|--------|-------|---------|-------|
| VER-01 | 15 | Validated | `fetchCrossRefTitle` extracts `message.title[0]` via Zod schema |
| VER-02 | 15 | Validated | `CROSSREF_USER_AGENT = "annas-mcp-ts/1.4 (mailto:noreply@example.com)"` |
| VER-03 | 15 | Validated | `AbortSignal.timeout(5000)` + injectable fetch for isolation |
| VER-04 | 16 | Validated | `normalizeTitle`: `.toLowerCase().replace(/[^a-z0-9\s]/g, "")` |
| VER-05 | 16 | Validated | `JACCARD_THRESHOLD = 0.5`; both-empty → high; one-empty → low |
| VER-06 | 16 | Validated | `if (crossrefTitle === null) return "unverified"` |
| VER-07 | 17 | Validated (design upgraded post-phase) | Hard-error gating model delivered; original additive field spec superseded intentionally |
| VER-08 | 17 | Validated (design upgraded post-phase) | Batch gating: `{ doi, error }` on low/unverified; original additive field spec superseded |
| VER-09 | 17 | Validated (design upgraded post-phase) | Unverified → `isError: true`; no server crash on CrossRef failure |
| VER-10 | 17 | Validated | Net change to callers: zero |
| VER-11 | 17 | Validated | 114 tests pass (99 pre-existing + 15 new across phases 15–17) |

---

## Future Requirements (Deferred)

- Configurable confidence threshold (currently hardcoded at 0.5 Jaccard)
- Author cross-validation in addition to title (CrossRef also returns authors)
- `article_search` DOI auto-detection (query starting with `10.` routes to DOI lookup — parity with Go upstream)

---

## Out of Scope

- Blocking downloads on low confidence — implemented as hard error, so this was effectively resolved with the post-phase upgrade
- Author or journal validation — title alone is sufficient signal for v1.4
- Caching CrossRef responses — CrossRef is fast and free; caching adds complexity without clear need
- `book_download` verification — books use MD5 hash not DOI; no CrossRef equivalent

---

_Archived: 2026-05-28 — v1.4 milestone complete_
